#ifndef _UTILES_H
#define _UTILES_H

/* system includes */
#include <stdio.h>
#include <stdint.h>

/* Prototypes defs */
extern void print_buffer(int count, char *buff);
extern char *ip_ntoa(uint32_t ip);

#endif
